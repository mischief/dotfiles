# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi

## Path setup

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
  fi

export TZ=US/Pacific
export LANG=en_US.UTF-8
export LC_CTYPE=en_US.UTF-8
export PAGER=less
export PLAN9=/usr/local/plan9
export PATH=$HOME/bin:/srv/bin:$PATH:$PLAN9/bin

export BROWSER=xombrero
export EDITOR=vim
export unset FCEDIT VISUAL

export GS_FONTPATH=$PLAN9/postscript/font

export font=$PLAN9/font/lucm/unicode.9.font

export home=$HOME
export prompt='$H=; '
export user=$USER

# Go
export GOROOT=/srv/go
export GOBIN=$GOROOT/bin
export GOPATH=$HOME/code/go
export PATH=$GOBIN:$GOPATH/bin:$PATH

# Privoxy
export http_proxy=http://127.0.0.1:8118/

#export TZ PLAN9 PATH BROWSER EDITOR GS_FONTPATH font home prompt user GOROOT GOBIN GOPATH http_proxy

# fns
function sprunge {
  printf '%s%s\n' "$(curl -sF 'sprunge=<-' http://sprunge.us/)" "${*:+?$*}"
}

alias mp3dl='youtube-dl -t --extract-audio --audio-format mp3'

function geoip {
  curl -s -o - http://freegeoip.net/csv/$1
}

# hack for del/backspace?
#bind -m '^X3'=^X
#bind '^X~'=delete-char-forward

ulimit -c unlimited

# vi editing mode
set -o vi

#. $HOME/.bashrc
